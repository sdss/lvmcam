
camera_type: araviscam  # types: skymakercam, araviscam

basename: "lvm.skye.agcam.{camera.name}_{num:08d}.fits"
dirname: "${LVMT_DATA_ROOT:-$HOME/data}/lvm/skye/agcam/{camera.name}/{date.strftime('%Y%m%d')}"

camera_params: !include lvm.camera_param.agcam.yml

scraper: !include lvm.scraper.agcam.yml

cameras:
    east:
        descr: "Guider Camera Skye East"

        # araviscam
        uid: 19283182
        ip: "192.168.80.64"

        # skymakercam
        instpar: lvm_skye_age_cam

    west:
        descr: "Guider Camera Skye West"

        # araviscam
        uid: 19283183
        ip: "192.168.80.65"

        ## skymakercam
        instpar: lvm_skye_agw_cam

actor:
    name: lvm.skye.agcam
    host: localhost
    port: 5672
    log_dir: '~/tmp/log'
    #default_cameras:
       #- west
